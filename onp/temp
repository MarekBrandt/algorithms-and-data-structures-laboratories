#include <stack>
#include <iostream>
#include <string>

using namespace std;

int main() {
    int howManyInputs, letterIndex;
    stack <char> givenLetters;
    char letter;
    scanf_s("%d", &howManyInputs);
    string expression;

    for(int i = 0; i < howManyInputs; i++) {
        cin >> expression;
        letterIndex = 0;
        while(expression[letterIndex] != '\n') {
            letter = expression[letterIndex];

            switch (letter) {
                case 'f':
                case 't':
                    givenLetters.push(letter);
                    break;
                case 'N':
                    break;
                case 'A':
                    break;
                case 'K':
                    break;
                default:
                    break;
            }
        }
    }

    return 0;
}
